<h1>Nuevo Aljibe</h1>

<div class="row">
  <div class="well bs-component">
    <%= form_for @tank, :html => {:class => "form-horizontal"} do |f| %>
      <fieldset>
      <%= render partial: "form", locals: {f: f} %>      
      <div class="form-group">
          <div class="col-lg-10 col-lg-offset-2">
            <%= f.submit "Alta Aljibe", class: "btn btn-primary" %>
            <%= link_to "Volver atrás", tanks_path, class: "btn btn-default" %>
          </div>
        </div>
      </fieldset>
    <% end %>
  </div>
</div>